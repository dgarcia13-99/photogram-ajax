//Update follow/un-follow/un-request button
$(".sender_<%= @follow_request.sender_id %>_recipient_<%= @follow_request.recipient_id %>_follow_unfollow")
  .html("<%= j(render 'follow_requests/follow_unfollow', sender: @follow_request.sender, recipient: @follow_request.recipient) %>");
